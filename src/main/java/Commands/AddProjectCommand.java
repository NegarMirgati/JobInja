package Commands;
/*
import Repositories.ProjectRepo;
import Entities.*;
import java.util.HashMap;

public class AddProjectCommand implements Command{
    private String id;
    private String title;
    private String description;
    private String imageURL;
    private HashMap<String, Skill> skills;
    private int budget;
    private long deadline;

    public AddProjectCommand(String id, String title, String description, String imageURL, int budget, long deadline, HashMap<String, Skill> skills) {
        this.id = id;
        this.title = title;
        this.description = description;
        this.imageURL = imageURL;
        this.budget = budget;
        this.deadline = deadline;
        this.skills = new HashMap<String, Skill> (skills);
    }

    public void execute() {

        ProjectRepo.addProject(this.id, this.title, this.description, this.imageURL, this.budget, this.deadline, this.skills);
    }
}
*/